Listing Commands In Constellinux Is Quite Simple. You Simply List the Commands, with a Semi-Colon and a space between them, like this:

```
ls; clear
```

This would list files in the working directory, then clear the terminal, but because of the ordering of outputs going after direct actions, acually clears terminal and leaves just the ls output. Useful!